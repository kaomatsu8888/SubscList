# 変更履歴 (Changelog)

このプロジェクトの主な変更点をまとめています。
形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に準拠し、
バージョンは [セマンティック バージョニング](https://semver.org/lang/ja/) を採用しています。

> **補足**: 各リリースで Service Worker のキャッシュ名（`sw.js` の `CACHE` 定数）を
> 更新しています。これによりアプリ更新時に古いキャッシュが破棄され、最新版が反映されます。

---

## [Unreleased]

SW キャッシュ: `subscbox-v12`

### 変更 (Changed)
- **カスタム並び替えを「並び替えモード＋ドラッグ&ドロップ」に刷新**:
  常時表示の ↑↓ ボタンを廃止。カスタム選択時は「並び替え」ボタンを表示し、
  押すと編集モードに入りドラッグハンドル（≡）で並び替え。「完了」で通常表示に戻る。
  タッチ・マウス両対応、端でのオートスクロール付き。

### 修正 (Fixed)
- **カスタム並び替えで先頭にスクロールしてしまう不具合を修正**: 並び替え後も
  スクロール位置を維持するように（並び替えモード化と合わせて改善）。

### 追加 (Added)
- **プリセット解約ガイド**: 主要サービス（Netflix / Spotify / Amazon プライム /
  YouTube Premium / Disney+ / Apple / ChatGPT / U-NEXT / DAZN / Hulu）について、
  サービス名から自動でマッチし、詳細画面の「解約について」に解約手順と解約ページボタンを表示。
  ユーザーが URL をコピペしなくても解約導線が得られる。

### 変更 (Changed)
- **URL 欄の役割を明確化**: ラベルを「解約ページのURL」に変更し、
  「主要サービスは自動表示・それ以外で控えたい場合に（任意）」とヒントを追加。

### 分析グラフの改善 (Changed)
- **支出の推移をカテゴリ別の積み上げ棒グラフに**: 合計だけでなく各カテゴリの比率が
  分かるように。過去月は合計値のみ保存のため、現在のカテゴリ比率で按分した目安を表示。
- **カテゴリ別内訳に金額・割合つきリストを追加**: ドーナツの下に、各カテゴリの
  色・名前・割合(%)・月額を一覧表示。
- **カテゴリ別内訳を金額の多い順に並べ替え**。

### 追加 (Added)
- **初回請求日に「今日」ボタン**: 登録・編集フォームの初回請求日入力の横に、
  ワンタップで今日の日付をセットできるボタンを追加。
- **解約済みサブスクの詳細画面から再開（復元）**: 解約済みのサブスクを開くと
  「このサブスクを再開する」ボタンが表示され、設定の奥に入らなくても復元できるように。
- **詳細画面に「解約済み」バッジ**: 解約済みのサブスクは状態が一目で分かるよう表示。

### 変更 (Changed)
- **削除確認の文言を改善**: 「履歴ごと完全に削除され、復元できません。再開予定なら
  「解約済み」が便利です。」と、削除と解約の使い分けを案内するように。

---

## [1.1.0] - 2026-06-08

v1.0.0 リリース後に追加・改善した機能をまとめたバージョンです。
SW キャッシュ: `subscbox-v6`

### 追加 (Added)
- **JSON インポート機能**: エクスポートした JSON を読み込んでデータを復元できるように。
  破損ファイルの検証（必須フィールドのチェック）付き。
- **全データの一括削除**: 設定 →「危険な操作」から、確認ダイアログ付きで全データを削除。
- **サブスクの複製機能**: 詳細画面の「複製」から、値をコピーした状態で新規登録フォームを開く。
  似たサブスク（家族分・別プランなど）の登録が簡単に。
- **カテゴリを 7 → 13 種類に拡張**: ゲーム / 読書・雑誌 / 学習・教育 / クラウド・保存 /
  ショッピング / 美容・健康 を追加。既存ユーザーのデータには自動マイグレーションで補完。
- **詳細画面に利用実績を表示**: 「利用期間」（契約開始からの経過）と
  「これまでの支払額」（請求回数 × 金額、元通貨）を追加。解約判断の材料に。
- **為替更新ボタンの改善**: 外貨サブスクがある場合のみ表示し、最終更新時刻
  （例: 「為替: 3時間前」）を併記。古い場合は更新を促す表示。
- **分析の前月比**: 前月の比較データが無い月は「比較データがまだありません」と明示。

### 変更 (Changed)
- **ホームの合計額をタブ連動に**: すべて / 月額 / 年額のタブ切替で、
  ヒーローカードの合計額・件数・ラベルが連動するように。
- **保存後の遷移を整理**: サブスクの新規登録・編集の保存後はホームへ遷移。
- **詳細画面の「戻る」をホーム固定に**: 編集画面に誤って戻る不具合を解消。
- **保存トーストの位置調整**: PWA（ホーム画面追加）でノッチ等に隠れないよう safe-area 対応。
- **登録フォームのカラー選択を廃止**: アイコン背景色はカテゴリの色を自動で流用するように。
  登録の手間を削減し、カテゴリと色の一貫性を確保。

### 削除 (Removed)
- 登録フォームの「カラー」選択 UI（および未使用の関連 CSS）。

---

## [1.0.0] - 2026-06

初回リリース。

### 追加 (Added)
- サブスクの追加・編集・解約・削除
- 月額換算合計・年間換算・1 日あたりの自動計算
- 次回請求日・残り日数・進捗バーの表示
- カレンダーで請求日を可視化
- Chart.js による支出推移・カテゴリ別グラフ
- ルールベースの「サブスク健康診断」
- 起動時リマインドバナー通知
- 外貨対応（JPY / USD / EUR / TRY / NGN / INR）
- ライト / ダーク / 自動テーマ
- JSON エクスポート
- PWA（ホーム画面追加・オフライン動作）
